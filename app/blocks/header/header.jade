include ../banner/banner
include ../top-menu/top-menu
include ../search-form/search-form
include ../info-counter/info-counter

mixin header()
	+banner({
		"primary": "RESERVE A BOAT NOW!",
		"secondary": "and set your sails into paradise!"})._header
	+b.header&attributes(attributes)
		+top-menu
		.container
			+search-form
			+info-counter
